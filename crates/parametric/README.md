# Parametric

byo validator or use ours

must install Zoo CLI -- https://github.com/KittyCAD/cli/releases

## Development

After making changes to any KCL files, run the formatter:

```bash
zoo kcl format -w <filename>
```

For example:
```bash
zoo kcl format -w src/main.kcl
```

This ensures consistent code style across all KCL files in the project.
