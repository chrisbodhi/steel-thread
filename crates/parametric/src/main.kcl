import plateThickness, boltDiameter, boltSpacing, bracketHeight, bracketWidth, pinDiameter, pinCount from "params.kcl"
import plate from "plate.kcl"

@settings(defaultLengthUnit = mm, kclVersion = 1.0)

plate(plate_thickness = plateThickness, bolt_diameter = boltDiameter, bolt_spacing = boltSpacing, bracket_height = bracketHeight, bracket_width = bracketWidth, pin_diameter = pinDiameter, pin_count = pinCount)
